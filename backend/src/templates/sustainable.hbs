<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
  @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Poppins:wght@300;400;500;600;700&display=swap');

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'Poppins', -apple-system, sans-serif;
    color: #1b4332;
    line-height: 1.6;
    font-size: 11pt;
    background: #ffffff;
  }

  /* ===== FRONT PAGE ===== */
  .front-page {
    page-break-after: always;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 20px 10px;
    border: 2px solid #b7e4c7;
    border-radius: 12px;
    background: #ffffff;
    position: relative;
  }

  /* Top Accent Line */
  .front-page::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 10px;
    background: linear-gradient(90deg, #1b4332 0%, #52b788 100%);
    border-top-left-radius: 10px;
    border-top-right-radius: 10px;
  }

  .logo-top-row {
    display: flex; align-items: center; justify-content: center;
    gap: 20px; margin-bottom: 30px; width: 100%;
  }

  .logo-top-row img { object-fit: contain; border: 1px solid #b7e4c7; border-radius: 8px; padding: 4px; background: #fff; }

  .main-event-logo { text-align: center; margin-bottom: 25px; }
  .main-event-logo img { max-width: 220px; max-height: 150px; object-fit: contain; border-radius: 10px; border: 1px solid #b7e4c7; padding: 6px; background: #fff; }

  .institution-name { font-family: 'Outfit', sans-serif; font-size: 15pt; font-weight: 800; color: #1b4332; letter-spacing: 0.5px; text-transform: uppercase; margin-bottom: 6px; }
  .department-name { font-size: 11pt; font-weight: 500; color: #2d6a4f; text-transform: uppercase; margin-bottom: 30px; }

  .event-title { font-family: 'Outfit', sans-serif; font-size: 20pt; font-weight: 800; color: #1b4332; margin-bottom: 8px; line-height: 1.25; }
  .event-subtitle { font-size: 12pt; font-weight: 400; color: #2d6a4f; margin-bottom: 25px; max-width: 90%; }

  .details-table {
    width: 90%; border-collapse: collapse; margin: 10px auto;
    background: #f4faf6; border-radius: 8px; overflow: hidden;
    border: 1px solid #b7e4c7;
  }
  .details-table td { padding: 10px 15px; font-size: 9.5pt; border-bottom: 1px solid #b7e4c7; text-align: left; }
  .details-table tr:last-child td { border-bottom: none; }
  .detail-key { font-weight: 700; color: #1b4332; width: 35%; background: #e9f5ed; }
  .detail-value { color: #2d3748; }

  .eco-divider {
    text-align: center;
    font-size: 12pt;
    letter-spacing: 6px;
    margin-top: 15px;
    opacity: 0.8;
  }

  /* ===== CONTENT ===== */
  .content-card {
    background: #ffffff;
    padding: 15px 0;
    margin: 15px 0;
    page-break-inside: avoid;
  }

  .section-heading { 
    font-family: 'Outfit', sans-serif;
    font-size: 13pt; 
    font-weight: 700; 
    color: #1b4332; 
    margin-bottom: 14px; 
    padding-bottom: 4px; 
    border-left: 4px solid #52b788; 
    padding-left: 10px;
  }
  
  .section-content p { text-align: justify; margin-bottom: 12px; color: #2d3748; font-size: 10pt; }
  .section-content ul { margin-left: 20px; margin-bottom: 12px; }
  .section-content ul li { margin-bottom: 6px; color: #2d3748; font-size: 10pt; }
  .section-content ul li::marker { color: #52b788; }

  .image-rectangle { 
    background: #ffffff; 
    border-radius: 8px; 
    padding: 8px; 
    margin: 15px 0; 
    border: 1px solid #b7e4c7; 
    text-align: center; 
    page-break-inside: avoid; 
  }
  .image-rectangle img { width: 100%; max-height: 380px; object-fit: cover; border-radius: 6px; }
  .img-caption { text-align: center; font-size: 8pt; color: #2d6a4f; margin-top: 6px; font-style: italic; }

  /* ===== IMAGE GRID LAYOUTS ===== */
  .section-grid {
    display: block;
  }
  .section-grid.has-images {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 20px;
    align-items: start;
  }
  .section-text { flex: 1; }
  .section-photos { flex: 1; }
  .image-container { display: grid; gap: 10px; width: 100%; }
  .img-full { grid-template-columns: 1fr; }
  .img-full img { width: 100%; max-height: 280px; object-fit: cover; border-radius: 8px; border: 1px solid #b7e4c7; }
  .img-side-by-side { grid-template-columns: 1fr 1fr; }
  .img-side-by-side img { width: 100%; height: 140px; object-fit: cover; border-radius: 8px; border: 1px solid #b7e4c7; }
  .img-grid { grid-template-columns: 1fr 1fr; }
  .img-grid img { width: 100%; height: 100px; object-fit: cover; border-radius: 6px; border: 1px solid #b7e4c7; }
  .image-wrapper { position: relative; }

  /* ===== BUDGET TABLE ===== */
  .budget-table { width: 100%; border-collapse: collapse; margin: 15px 0; font-size: 9.5pt; border: 1px solid #b7e4c7; border-radius: 8px; overflow: hidden; }
  .budget-table th { background: linear-gradient(90deg, #1b4332, #2d6a4f); color: white; padding: 10px 12px; text-align: left; font-weight: 600; font-size: 8.5pt; text-transform: uppercase; }
  .budget-table td { padding: 8px 12px; border-bottom: 1px solid #b7e4c7; }
  .budget-table tr:nth-child(even) { background: #f4faf6; }
  .budget-total { font-weight: 700; background: #e9f5ed !important; color: #1b4332; }

  /* ===== GALLERY ===== */
  .gallery-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin: 15px 0; }
  .gallery-grid img { width: 100%; height: 150px; object-fit: cover; border-radius: 8px; border: 1px solid #b7e4c7; }

  /* ===== SIGNATURE CARD ===== */
  .signature-section {
    margin-top: 40px;
    padding-top: 20px;
    border-top: 2px dashed #b7e4c7;
    page-break-inside: avoid;
  }
  .sig-container {
    display: flex;
    justify-content: space-between;
    margin-top: 40px;
  }
  .sig-box {
    text-align: center;
    width: 30%;
  }
  .sig-line {
    border-top: 1px solid #1b4332;
    margin-bottom: 6px;
  }
  .sig-title {
    font-size: 9.5pt;
    font-weight: 600;
    color: #1b4332;
  }
  .sig-subtitle {
    font-size: 8pt;
    color: #2d6a4f;
  }

  .report-footer {
    text-align: center;
    margin-top: 50px;
    padding-top: 15px;
    border-top: 1px solid #b7e4c7;
    font-size: 8pt;
    color: #52b788;
  }

  @media print { 
    .content-card { page-break-inside: avoid; } 
    .front-page { page-break-after: always; } 
  }
</style>
</head>
<body>

  <!-- FRONT PAGE -->
  <div class="front-page">
    {{#if report.frontPage.logos.length}}
      <div class="logo-top-row">
        {{#each report.frontPage.logos}}
          {{#unless @last}}
            <img src="{{this}}" alt="Logo" style="width: {{logoSize ../report.frontPage.logos.length}}px; height: {{logoSize ../report.frontPage.logos.length}}px;">
          {{/unless}}
        {{/each}}
      </div>
    {{/if}}

    {{#if report.frontPage.institutionName}}<div class="institution-name">{{report.frontPage.institutionName}}</div>{{/if}}
    {{#if report.frontPage.departmentName}}<div class="department-name">{{report.frontPage.departmentName}}</div>{{/if}}

    {{#if report.frontPage.logos.length}}
      <div class="main-event-logo"><img src="{{lastLogo report.frontPage.logos}}" alt="Event Logo"></div>
    {{/if}}

    <div class="event-title">{{report.frontPage.eventTitle}}</div>
    {{#if report.frontPage.eventSubtitle}}<div class="event-subtitle">{{report.frontPage.eventSubtitle}}</div>{{/if}}

    {{#if report.frontPage.eventDetails.length}}
      <table class="details-table">
        {{#each report.frontPage.eventDetails}}<tr><td class="detail-key">{{this.key}}</td><td class="detail-value">{{this.value}}</td></tr>{{/each}}
      </table>
    {{/if}}

    <div class="eco-divider">🌿 🌿 🌿 🌿 🌿 🌿 🌿 🌿 🌿</div>
  </div>

  <!-- CONTENT SECTIONS -->
  {{#each sections}}
    {{#unless (eq this.type "BUDGET")}}{{#unless (eq this.type "GALLERY")}}
      <div class="content-card">
        <h2 class="section-heading">{{this.heading}}</h2>
        <div class="section-grid {{#if this.images.length}}has-images{{/if}}">
          <div class="section-text">
            <div class="section-content">
              {{#each this.content.paragraphs}}<p>{{this}}</p>{{/each}}
              {{#if this.content.bullets.length}}<ul>{{#each this.content.bullets}}<li>{{this}}</li>{{/each}}</ul>{{/if}}
              {{#if this.content.richText}}{{{this.content.richText}}}{{/if}}
            </div>
          </div>
          {{#if this.images.length}}
            <div class="section-photos">
              <div class="image-container {{imageGridClass this.imageCount}}">
                {{#each this.images}}
                  <div class="image-wrapper">
                    <img src="{{this.url}}" alt="{{this.caption}}">
                    {{#if this.caption}}<div class="img-caption">{{this.caption}}</div>{{/if}}
                  </div>
                {{/each}}
              </div>
            </div>
          {{/if}}
        </div>
      </div>
    {{/unless}}{{/unless}}
  {{/each}}

  <!-- GALLERY SECTIONS -->
  {{#each sections}}
    {{#if (eq this.type "GALLERY")}}
      <div class="content-card">
        <h2 class="section-heading">{{this.heading}}</h2>
        <div class="gallery-grid">
          {{#each this.images}}
            <div class="image-wrapper">
              <img src="{{this.url}}" alt="{{this.caption}}">
              {{#if this.caption}}<div class="img-caption">{{this.caption}}</div>{{/if}}
            </div>
          {{/each}}
        </div>
      </div>
    {{/if}}
  {{/each}}

  <!-- BUDGET SUMMARY -->
  {{#if budgets.length}}
    <div class="content-card">
      <h2 class="section-heading">Budget Summary</h2>
      <table class="budget-table">
        <thead><tr><th style="width: 5%">#</th><th>Item</th><th>Category</th><th style="width: 10%">Qty</th><th style="width: 20%">Unit Cost</th><th style="width: 20%">Total</th></tr></thead>
        <tbody>
          {{#each budgets}}<tr><td>{{@index}}</td><td>{{this.item}}</td><td>{{this.category}}</td><td>{{this.quantity}}</td><td>{{formatCurrency this.unitCost}}</td><td>{{formatCurrency this.totalCost}}</td></tr>{{/each}}
          <tr class="budget-total"><td colspan="5" style="text-align:right;"><strong>Grand Total</strong></td><td><strong>{{formatCurrency budgetTotal}}</strong></td></tr>
        </tbody>
      </table>
    </div>
  {{/if}}

  <!-- SIGN-OFF SECTION -->
  <div class="signature-section">
    <div class="sig-container">
      <div class="sig-box">
        <div class="sig-line"></div>
        <div class="sig-title">{{report.createdBy.name}}</div>
        <div class="sig-subtitle">Report Prepared By<br>{{report.createdBy.department}}</div>
      </div>
      <div class="sig-box">
        <div class="sig-line"></div>
        <div class="sig-title">Coordinator</div>
        <div class="sig-subtitle">Event coordinator</div>
      </div>
      <div class="sig-box">
        <div class="sig-line"></div>
        <div class="sig-title">Head of Department</div>
        <div class="sig-subtitle">Department Head Approval</div>
      </div>
    </div>
  </div>

  <div class="report-footer">
    Report generated on {{generatedAt}} | Smart Event Report System
  </div>

</body>
</html>
